name: seven_columns
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_spinkit: ^5.0.0
  cloud_firestore: 
  cupertino_icons: ^1.0.0

  flutter_bloc: ^6.1.2
  equatable:   
  dartz: 
  
  
  google_fonts:   
  firebase_auth:   
  firebase: ^8.0.0

  path_provider: 
  provider:
  flex_color_scheme:
  toast: ^0.1.5
  
  shared_preferences: 
  curved_navigation_bar: 
  image_picker: 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.8.1
  mockito: ^4.1.3

#flutter pub run flutter_launcher_icons:main
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/7Columns.jpg"

flutter:
  uses-material-design: true

  assets:
   - assets/
  #   - images/a_dot_ham.jpeg
